# Security Baseline Assessment Report

**Date:** {{ report_date }}
**Generated by:** Ansible security-audit.yml

---

## Executive Summary

| Host | PASS | FAIL | WARN | Critical | High | Medium | Low |
|------|------|------|------|----------|------|--------|-----|
{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% if host_findings | length > 0 %}
| {{ host }} | {{ host_findings | selectattr('status', 'equalto', 'PASS') | list | length }} | {{ host_findings | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('status', 'equalto', 'WARN') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'CRITICAL') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'HIGH') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'MEDIUM') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'LOW') | selectattr('status', 'equalto', 'FAIL') | list | length }} |
{% endif %}
{% endfor %}

---

## Per-Host Detail

{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% if host_findings | length > 0 %}
### {{ host }}

**OS:** {{ hostvars[host]['ansible_distribution'] | default('N/A') }} {{ hostvars[host]['ansible_distribution_version'] | default('') }}
**Kernel:** {{ hostvars[host]['ansible_kernel'] | default('N/A') }}

| Status | Severity | Check | Detail |
|--------|----------|-------|--------|
{% for finding in host_findings | sort(attribute='severity') %}
| {{ finding.status }} | {{ finding.severity }} | {{ finding.check }} | {{ finding.detail }} |
{% endfor %}

{% endif %}
{% endfor %}

---

## Prioritized Remediation Roadmap

### CRITICAL Findings

{% set ns = namespace(has_critical=false) %}
{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'CRITICAL') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_critical = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_critical %}
No critical findings.
{% endif %}

### HIGH Findings

{% set ns = namespace(has_high=false) %}
{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'HIGH') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_high = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_high %}
No high findings.
{% endif %}

### MEDIUM Findings

{% set ns = namespace(has_medium=false) %}
{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'MEDIUM') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_medium = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_medium %}
No medium findings.
{% endif %}

### LOW Findings

{% set ns = namespace(has_low=false) %}
{% for host in groups['linux'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'LOW') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_low = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_low %}
No low findings.
{% endif %}

---

## Patch Status Matrix

| Host | Upgradable Pkgs | Unattended-Upgrades | Reboot Required | Kernel Update | Apt Cache Age |
|------|-----------------|---------------------|-----------------|---------------|---------------|
{% for host in groups['linux'] | default([]) %}
{% set pf = hostvars[host]['patches_findings'] | default([]) %}
{% if pf | length > 0 %}
{% set upgradable = pf | selectattr('check', 'equalto', 'Available updates') | map(attribute='detail') | first | default('N/A') %}
{% set unattended = pf | selectattr('check', 'equalto', 'Unattended-upgrades active') | map(attribute='status') | first | default('N/A') %}
{% set reboot = pf | selectattr('check', 'equalto', 'Reboot required') | map(attribute='detail') | first | default('N/A') %}
{% set kernel = pf | selectattr('check', 'equalto', 'Kernel update available') | map(attribute='status') | first | default('N/A') %}
{% set apt_age = pf | selectattr('check', 'equalto', 'Apt cache freshness') | map(attribute='detail') | first | default('N/A') %}
| {{ host }} | {{ upgradable }} | {{ unattended }} | {{ reboot }} | {{ kernel }} | {{ apt_age }} |
{% endif %}
{% endfor %}

---

## Recommended Playbook Runs

Based on the findings above, consider running the following playbooks to remediate issues:

| Playbook | Purpose | Command |
|----------|---------|---------|
| server-baseline.yml | Base hardening (SSH, UFW, fail2ban, auditd) | `ansible-playbook -i inventory.ini server-baseline.yml` |
| cis-benchmark-phase1.yml | CIS Level 1 (SSH crypto, sysctl, file perms, audit rules) | `ansible-playbook -i inventory.ini cis-benchmark-phase1.yml` |
| cis-benchmark-phase2.yml | CIS Level 2 (AIDE, banners, advanced audit, PAM) | `ansible-playbook -i inventory.ini cis-benchmark-phase2.yml` |

---

*Report generated automatically by security-audit.yml*
