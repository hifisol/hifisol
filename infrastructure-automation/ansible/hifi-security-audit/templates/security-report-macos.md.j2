# macOS Security Baseline Assessment Report

**Date:** {{ report_date }}
**Generated by:** Ansible security-audit-macos.yml

---

## Executive Summary

| Host | PASS | FAIL | WARN | Critical | High | Medium | Low |
|------|------|------|------|----------|------|--------|-----|
{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% if host_findings | length > 0 %}
| {{ host }} | {{ host_findings | selectattr('status', 'equalto', 'PASS') | list | length }} | {{ host_findings | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('status', 'equalto', 'WARN') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'CRITICAL') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'HIGH') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'MEDIUM') | selectattr('status', 'equalto', 'FAIL') | list | length }} | {{ host_findings | selectattr('severity', 'equalto', 'LOW') | selectattr('status', 'equalto', 'FAIL') | list | length }} |
{% endif %}
{% endfor %}

---

## Per-Host Detail

{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% if host_findings | length > 0 %}
### {{ host }}

**macOS:** {{ hostvars[host]['ansible_distribution_version'] | default('N/A') }}
**Kernel:** {{ hostvars[host]['ansible_kernel'] | default('N/A') }}

| Status | Severity | Check | Detail |
|--------|----------|-------|--------|
{% for finding in host_findings | sort(attribute='severity') %}
| {{ finding.status }} | {{ finding.severity }} | {{ finding.check }} | {{ finding.detail }} |
{% endfor %}

{% endif %}
{% endfor %}

---

## Prioritized Remediation Roadmap

### CRITICAL Findings

{% set ns = namespace(has_items=false) %}
{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'CRITICAL') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_items = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_items %}
No critical findings.
{% endif %}

### HIGH Findings

{% set ns = namespace(has_items=false) %}
{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'HIGH') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_items = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_items %}
No high findings.
{% endif %}

### MEDIUM Findings

{% set ns = namespace(has_items=false) %}
{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'MEDIUM') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_items = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_items %}
No medium findings.
{% endif %}

### LOW Findings

{% set ns = namespace(has_items=false) %}
{% for host in groups['macos'] | default([]) %}
{% set host_findings = hostvars[host]['all_findings'] | default([]) %}
{% for finding in host_findings | selectattr('severity', 'equalto', 'LOW') | selectattr('status', 'equalto', 'FAIL') | list %}
{% set ns.has_items = true %}
- **{{ host }}** - {{ finding.check }}: {{ finding.detail }}
  - Fix: `{{ finding.fix }}`
{% endfor %}
{% endfor %}
{% if not ns.has_items %}
No low findings.
{% endif %}

---

## Patch Status

| Host | macOS Version | Updates Available | Auto-Check | Auto-Download | Critical Updates | Auto-Install |
|------|---------------|-------------------|------------|---------------|------------------|--------------|
{% for host in groups['macos'] | default([]) %}
{% set pf = hostvars[host]['patches_findings'] | default([]) %}
{% if pf | length > 0 %}
{% set version = pf | selectattr('check', 'equalto', 'macOS version') | map(attribute='detail') | first | default('N/A') %}
{% set updates = pf | selectattr('check', 'equalto', 'Available updates') | map(attribute='status') | first | default('N/A') %}
{% set auto_check = pf | selectattr('check', 'equalto', 'Automatic update check') | map(attribute='status') | first | default('N/A') %}
{% set auto_dl = pf | selectattr('check', 'equalto', 'Automatic download') | map(attribute='status') | first | default('N/A') %}
{% set critical = pf | selectattr('check', 'equalto', 'Auto install critical updates') | map(attribute='status') | first | default('N/A') %}
{% set auto_inst = pf | selectattr('check', 'equalto', 'Auto install macOS updates') | map(attribute='status') | first | default('N/A') %}
| {{ host }} | {{ version }} | {{ updates }} | {{ auto_check }} | {{ auto_dl }} | {{ critical }} | {{ auto_inst }} |
{% endif %}
{% endfor %}

---

*Report generated automatically by security-audit-macos.yml*
